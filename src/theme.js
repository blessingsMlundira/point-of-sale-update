import { createTheme }
from "@mui/material/styles";

export default createTheme({

 palette: {
   mode: "light",
   primary: {
     main: "#0078D4"
   }
 },

 shape: {
   borderRadius: 12
 },

 typography: {
   fontFamily: "Segoe UI"
 }

});